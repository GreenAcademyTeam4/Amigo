<?xml version="1.0" encoding="UTF-8" ?>
<!-- mapper DTD 선언 -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.amigo_project.repository.interfaces.ChatRepository">

    <select id="findEmoticonList" resultType="com.example.amigo_project.repository.model.chat.Emoticon">
        select * from emoticon_tb
    </select>

    <!--  채팅방 생성  -->
    <insert id="createChatRoom">
        INSERT INTO chat_room_tb(user_id, friend_id)
        VALUES (#{userId}, #{friendId})
    </insert>

    <!-- 사용자 ID와 친구 ID로 채팅방 조회 -->
    <select id="findChatRoomByUserIdANDFriendId" resultType="com.example.amigo_project.repository.model.chat.ChatRoom">
        SELECT id, user_id, friend_id, last_message_date
        FROM chat_room_tb
        WHERE (user_id = #{userId} AND friend_id = #{friendId})
        OR (user_id = #{friendId} AND friend_id = #{userId})
    </select>


    <!-- roomId로 채팅 로그 조회 -->
    <select id="findChatLogById" resultType="com.example.amigo_project.dto.chat.ChatLogDTO">
        SELECT *
        FROM chat_log_tb AS c
        JOIN user_tb AS u
        ON c.user_id = u.id
        WHERE room_id = #{roomId}
        ORDER BY created_at ASC
    </select>

    <!-- 채팅 로그 삽입 -->
    <insert id="createChatLog" parameterType="com.example.amigo_project.repository.model.chat.ChatLog">
        INSERT INTO chat_log_tb (room_id, user_id, type, message, created_at)
        VALUES (#{roomId}, #{userId}, #{type}, #{message}, #{createdAt})
    </insert>

    <select id="findLastMessageDate">
        SELECT last_message_date FROM chat_room_tb WHERE id = #{id}
    </select>

    <update id="updateLastMessageDate">
        UPDATE chat_room_tb SET last_message_date = #{lastMessageDate} WHERE id = #{id}
    </update>

</mapper>